# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/

# Throwaway working files (validation logs, exploratory output)
scratch/

# WIMS runtime state (derived; rebuildable)
wims.db
*.db-wal
*.db-shm
*.jsonl

# Local packet/QSO captures — real on-air data, not for the public repo.
# (The capture-based tests skip gracefully when these are absent.)
captures/

# N1MM / contest databases (never tracked; live outside the repo anyway)
*.s3db

# Claude Code local settings (machine-specific paths + permission grants)
.claude/settings.local.json

# Windows install machine-local artifacts
scripts/windows/install-log.txt
scripts/windows/python-path.txt
scripts/windows/seat-local.cmd
scripts/windows/seat-startup-log.txt
scripts/windows/agent-daemon-log.txt

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
